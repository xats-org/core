{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://pub.xats.org/vocabularies/blocks/list",
  "title": "List Block",
  "description": "A block for ordered or unordered lists",
  "type": "object",
  "properties": {
    "blockType": {
      "const": "https://pub.xats.org/vocabularies/blocks/list",
      "description": "The URI identifier for this block type"
    },
    "content": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["ordered", "unordered"],
          "description": "The type of list (ordered with numbers or unordered with bullets)"
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "https://pub.xats.org/schemas/0.1.0/xats.json#/definitions/SemanticText"
          },
          "description": "Array of list items, each as a SemanticText object"
        }
      },
      "required": ["type", "items"],
      "additionalProperties": false
    }
  },
  "required": ["blockType", "content"],
  "examples": [
    {
      "blockType": "https://pub.xats.org/vocabularies/blocks/list",
      "content": {
        "type": "unordered",
        "items": [
          {
            "runs": [
              { "type": "text", "text": "First item in the list" }
            ]
          },
          {
            "runs": [
              { "type": "text", "text": "Second item with " },
              { "type": "strong", "text": "bold text" }
            ]
          },
          {
            "runs": [
              { "type": "text", "text": "Third item" }
            ]
          }
        ]
      }
    },
    {
      "blockType": "https://pub.xats.org/vocabularies/blocks/list",
      "content": {
        "type": "ordered",
        "items": [
          {
            "runs": [
              { "type": "text", "text": "Step one" }
            ]
          },
          {
            "runs": [
              { "type": "text", "text": "Step two" }
            ]
          },
          {
            "runs": [
              { "type": "text", "text": "Step three" }
            ]
          }
        ]
      }
    }
  ]
}